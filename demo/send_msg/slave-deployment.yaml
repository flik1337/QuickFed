apiVersion: apps/v1
kind: Deployment
metadata:
  name: slave-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slave
  template:
    metadata:
      labels:
        app: slave
    spec:
      containers:
      - name: slave
        image: slave-image
